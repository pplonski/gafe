# gafe
Genetic Algorithm Feature Engineering

Simple algorithm for searching new features. It trys different combination of features with operators: `+`, `-`, `*` add it to your dataset and re-evaluate the classifier performance with new features.
